export enum NostrKind {
    META_DATA = 0,
    TEXT_NOTE = 1,
    RECOMMED_SERVER = 2,
    CONTACTS = 3,
    DIRECT_MESSAGE = 4
}

export enum NostrMessageType {
    EVENT = 'EVENT',
    REQUEST = 'REQ',
    CLOSE = 'CLOSE'
}